export default {
    plugins: {
        tailwindcss: {},
        /* Autoprefixer n'est pas décoratif ici : il ajoute les préfixes
         * que réclament les WebView Android anciennes encore très
         * présentes dans le parc. */
        autoprefixer: {},
    },
};
